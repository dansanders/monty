## Problem 13: Large Sum

use decimal

let _data = embed_bytes("data/13.txt")

fn solve(data: Bytes, N: Int) -> Int:
    total = data.lines().map(Decimal.parse).sum()
    (total >> (total.len() - N)).as_int()
