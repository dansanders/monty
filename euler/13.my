## Problem 13: Large Sum

use decimal

let _data = embed("data/13.txt")

fn solve(data: Bytes, N: Int) -> Int:
    parse_decimal = |s: Bytes| Decimal.try_parse(s).value_or_panic()
    total = data.lines().map(parse_decimal).sum()
    (total >> (total.len() - N)).try_as_int().value_or_panic()

@manual
test solution:
    expect solve(_data, 10) == 5_537_376_230
