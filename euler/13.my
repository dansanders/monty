## Problem 13: Large Sum

import decimal

_data = embed_bytes("data/13.txt")

fn solve(data: Bytes, N: Int) -> Int:
    total = data.lines().map(decimal::Decimal::parse).sum()
    (total >> (total.len() - N)).to_int()
